# MEMORY.md — project memory index (job-portal)

One line per memory: `- [Title](file.md) — when to read it`. Each memory lives in its own
`*.md` file in this folder (frontmatter: `name`, `description`, `metadata.type` =
`reference` | `feedback` | `project`). Update rather than duplicate; delete memories that
turn out wrong.

## Index

- (no memories yet)
